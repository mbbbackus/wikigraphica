{
  "name": "wikigraphica",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "bun run --hot server.ts",
    "build-queue": "bun run scripts/build-queue.ts",
    "worker": "bun run scripts/worker.ts",
    "build-edges": "bun run scripts/build-edges.ts"
  },
  "type": "module",
  "dependencies": {
    "cheerio": "^1.2.0"
  }
}
